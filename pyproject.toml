[project]
name = "nn_agentic"
version = "0.1.0"
description = "Neural Network to Agentic AI, but only from Papers & Books"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gymnasium>=1.1.1",
    "jax>=0.6.0",
    "mkdocs-material>=9.6.12",
]
